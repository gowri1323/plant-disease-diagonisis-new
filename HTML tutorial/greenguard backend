const express = require('express');
const app = express();
const PORT = 3000; // You can change this port number if you want

app.get('/', (req, res) => {
    res.send('Hello, GreenGuard!');
});

app.listen(PORT, () => {
    console.log(`Server is running on port ${PORT}`);
});
